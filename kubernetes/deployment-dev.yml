apiVersion: apps/v1
kind: Deployment
metadata:
  name: react-frontend-dev
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: react-frontend-dev
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: react-frontend-dev
    spec:
      containers:
        - image: gcr.io/project-id/react-frontend-dev
          imagePullPolicy: IfNotPresent
          name: react-frontend-dev
          ports:
            - containerPort: 4001
              protocol: TCP
---
  apiVersion: v1
  kind: Service
  metadata:
    name: react-frontend-dev
    namespace: default
  spec:
    ports:
      - port: 80
        protocol: TCP
        targetPort: 4001
    selector:
      app: react-frontend-dev
    sessionAffinity: None
    type: LoadBalancer